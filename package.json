{
  "name": "localstock-server",
  "version": "1.0.0",
  "description": "Express server for LocalStock — serves the frontend pages and handles session-based authentication.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.19.2",
    "express-session": "^1.18.0"
  }
}
